/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["storage.cloud.google.com", "storage.googleapis.com", "images.pexels.com"],
  },
};

export default nextConfig;
